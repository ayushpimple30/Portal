# Testing
Run `pytest`. The suite checks registration, duplicate prevention, login/authorization, progress persistence, quiz calculation, contact validation, feedback and administrator content creation.
